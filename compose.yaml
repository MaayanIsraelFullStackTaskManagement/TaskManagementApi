services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=mydatabase'
      - 'MYSQL_PASSWORD=mypassword'
      - 'MYSQL_ROOT_PASSWORD=mypassword'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306:3306'
